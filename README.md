# District WordPress Plugin

Custom Post Types for District CMS.

## Installation

1. Upload the `district-wordpress-plugin` folder to `/wp-content/plugins/`.
2. Activate the plugin through the 'Plugins' screen in WordPress admin.

## Development

This project uses PSR-4 autoloading via Composer. Run `composer install` to generate the autoloader.

## Contributing

Pull requests welcome.
